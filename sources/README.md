# sources

Mock source definitions and deterministic seeds. Run `make seed` from the project root. Generated files are disposable and ignored by Git; see [MESSINESS.md](MESSINESS.md) for file and record pointers. This explicit fixture rebuild is not permission for later runtime source writes.
